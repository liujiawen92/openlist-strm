# Core dependencies
flask>=2.0
werkzeug>=2.0
requests>=2.28

# Task scheduling (cron wrapper + fallback)
apscheduler>=6.0

# Optional: for async (Phase 2)
# httpx>=0.24
# aiohttp>=3.8

# For production server (Phase 2)
# gunicorn>=20.1